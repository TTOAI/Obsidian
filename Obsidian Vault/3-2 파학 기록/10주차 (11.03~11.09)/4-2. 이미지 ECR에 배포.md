
### 도커 이미지

- `.cache` 폴더도 포함시켜서 이미지를 빌드함
	- ECS에서 Fargate 실행 시에 인터넷에 접속해 파일을 다운로드하지 않도록 함

- Dockerfile
```dockerfile
FROM python:3.10-slim
  
WORKDIR /app
  
COPY . .
  
RUN pip install --no-cache-dir -r requirements.txt
  
CMD ["python", "imfact_batch_infer.py"]
```


### ECS 및 Task 정의 생성


### EventBridge 규칙 생성

