- NLP에서 텍스트를 기계가 처리할 수 있는 단위로 쪼개고 숫자로 변환하는 도구
- 역할
	1. 문장 분리 (Tokenization)
	2. ID 매핑 (Encoding)
		1. 각 토큰을 단어사전(vocab)의 인덱스로 변환
	3. 패딩과 마스킹
		- 문장 길이를 일정 길이(max_len)에 맞게 빈자리 패딩
		- 패딩된 부분은 학습에 영향이 없도록 attention mask로 표시
	4. 역변환 (Decoding)
		- 모델 출력의 숫자 ID -> 다시 텍스트로 복원 가능

