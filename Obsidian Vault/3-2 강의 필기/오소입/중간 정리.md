
- pushd, popd
- du
- touch로 시간 바꾸기
- 원본이 없더라도 soft link는 만들 수 있음 (원본이 생긴 후 정상 작동)
- export: export $A: subshell에서도 변수 A를 쓸 수 있게 함
- ||보다 &&가 우선순위 앞섬
- pipe: pipe는 subshell에서 실행됨
- | wc: 줄 수, 단어 수, 바이트 수 세어줌
- (...): subshell에서 실행됨
- 1>, 2>, &>
	- aaa 있고, zzz 없는 상황
	- ls aaa zzz 1>&2 2> err: aaa 그대로 출력, zzz오류는 err에 저장
	- ls aaa zzz 2> err 1>&2: zzz오류와 aaa출력이 차례대로 err에 저장
- if ./a.out; then echo Zero; else echo nonZero; fi
- test -e slink: symbolic link slink가 가리키는 대상이 없으면 실패(1) 반환
- sum = 0; for i in {3..9999..3}; do sum=$(($sum+$i)); done; echo $sum
-  for F in test.{0000..0099}.c; do echo "//  $F Comment" > $F; done
- sum=0; for (( i=3; i < 10000; i+=3)); do sum=$(($sum+$i)); done
- function cum_sum() { sum=0; for ((i=$2; i < $1; i+=$2)); do sum=$(($sum+$i)); done; echo $sum ;}
