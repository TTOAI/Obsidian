### 관련 정리

- 비트마스킹으로 일차원 배열 완전탐색 가능
```python
# 부분집합의 합
arr = [1, 2, 3, 4, 5]
n = len(arr)

for mask in range(0, (1 << n)):
	group_sum = 0
	for i in range(n):
		if mask & (1 << i):
			group_sum += arr[i]
```
